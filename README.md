# 準備
```bash
make clean
make zoom
```

# 使い方(サーバーの立て方)
```bash
./bin/zoom.exe (port)
```
# 使い方(クライアントの入り方)
```bash
./bin/zoom.exe (ip) (port)

voicechange <変えたい半音の数>
```
# できること
複数人で通話、チャットができるはず。
ボイスチェンジもできる。
